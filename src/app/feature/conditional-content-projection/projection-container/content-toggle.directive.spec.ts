import { ContentToggleDirective } from './content-toggle.directive';

describe('ContentToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentToggleDirective();
    expect(directive).toBeTruthy();
  });
});
